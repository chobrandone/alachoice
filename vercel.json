{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "npm install --include=dev",
  "buildCommand": "npm install --include=dev && npm run build --workspace @ala/types && npm run build --workspace @ala/backend && npm run build --workspace @ala/frontend",
  "outputDirectory": "apps/frontend/dist",
  "framework": null,
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api" },
    { "source": "/sitemap.xml", "destination": "/api" },
    { "source": "/health", "destination": "/api" },
    { "source": "/(.*)", "destination": "/index.html" }
  ],
  "crons": [{ "path": "/api/v1/cron/reminders", "schedule": "0 0 * * *" }]
}
