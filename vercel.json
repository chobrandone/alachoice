{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "rm -rf node_modules apps/backend/node_modules apps/frontend/node_modules packages/types/node_modules && npx --yes npm@10 install --include=dev",
  "buildCommand": "npm run build --workspace @ala/types && npm run build --workspace @ala/backend && npm run build --workspace @ala/frontend",
  "outputDirectory": "apps/frontend/dist",
  "framework": null,
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api" },
    { "source": "/sitemap.xml", "destination": "/api" },
    { "source": "/health", "destination": "/api" },
    { "source": "/(.*)", "destination": "/index.html" }
  ],
  "crons": [{ "path": "/api/v1/cron/reminders", "schedule": "0 0 * * *" }]
}
